Führe die QA-Routine für die Route `$ARGUMENTS` aus (bei leerem Argument: alle Routen). Nutze den Subagent `qa-runner`
gegen den Produktionsbuild, danach `text-fidelity` und `design-system-guard` für die betroffene Seite. Fasse das Ergebnis als
Tabelle zusammen (PASS/FAIL je Punkt) und nenne die drei wichtigsten Nachbesserungen. Keine Auto-Fixes ohne Rückfrage.
