Review den aktuellen Branch mit dem Subagent `pr-reviewer` gegen das Briefing `$ARGUMENTS` (Pfad in `docs/briefings/`).
Gib Blocker, Sollte, Nice und das Votum aus. Erstelle danach die dreisätzige Zusammenfassung für den Orga-Chat.
