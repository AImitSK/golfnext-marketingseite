# Seiten, Routen, Assets

Status pflegen, wenn eine Seite gebaut ist. Spalte „Bilder offen" listet die `Shot`-Platzhalter, die Fred mit echten Screenshots füllen muss.

> **Struktur v2 (Fred/Stefan, 07.09.2026 – Briefing 0023):** `/team`, `/ratgeber` (samt Unterrouten) und
> die zwölf `/module/<slug>` sind ersatzlos gestrichen; sie liefern 404, ohne Weiterleitung (die Seite
> war nie unter `www.golfnext.de` erreichbar). Der Blog heißt `/praxis`; die Adresse `/praxis` bleibt,
> obwohl der Menüpunkt unter „Über GolfNext" gewandert ist. Die zwölf Modulnamen stehen weiterhin in der
> Footer-Systemkarte, dort als Text ohne Link. Navigation siehe `docs/10-launch-umfang.md` § Navigation v2.

| Route | Quelle | Status | Bilder offen (Fred) |
|---|---|---|---|
| `/` | 3.1b (Neufassung v01) | **live** (2.2) · Rebuild aus 3.1b **gebaut** (0021) | Foto Fred (Zitat/Abschluss) als Porträt-Platzhalter; **die Artikel-Teaser sind kein offenes Bild mehr** (3.9, Briefing 0029) – sie kommen aus Sanity, das Titelbild pflegt die Redaktion im Studio, und fehlt es, steht der beschriftete `Shot`-Platzhalter „Bild folgt“; die übrigen Oberflächen (Clubwebsite-Demo mit schwebenden Karten, Bento-Visuals, Ein-Weg-Geräte) sind bewusst **schematische Illustrationen** (kein `Shot`, kein Foto nötig); **kein Modulstatus**; „Rehburg-Loccum/Pilotclub“-Wortlaut bleibt |
| `/plattform` | 3.2c (Neufassung v02) | **live** (2.3) · **FAQ-Abschnitt** vor dem Abschluss-CTA (Briefing 0030, Nachtrag 09.09.2026): geteilter Baustein `components/site/FaqSection.tsx` mit `topic: "plattform"`, Überschrift „Häufige Fragen zur Plattform.", Eyebrow „Klarheit vor dem Gespräch" (beides Seitentext in der Content-Datei). Zu diesem Thema liegt **noch keine Frage** in Sanity – der Abschnitt entfällt deshalb vollständig, ohne Leerfläche | keine – die Oberflächen (Browser-Demo, Marketing-CRM-Liste, Concierge-Chat, Geräterahmen der Scroll-Geschichte) sind bewusst **schematische Illustrationen** (kein `Shot`, kein echter Screenshot, kein Foto nötig) |
| `/plattform/so-arbeitet-golfnext` | 3.3b (Neufassung v01) | **live** (2.4, Briefing 0024) | keine – die Mail-Fenster und Zielgruppen-Strecken sind **schematische Illustrationen** (kein `Shot`, kein Screenshot); Beispiel-Adressen und -Namen illustrativ aus dem Mock, `aria-hidden`. **Kein Modulstatus.** Die frühere 3.3-Anforderung (CRM-Board- und Sequenz-Screenshots) entfällt mit der Neufassung |
| `/wachstum-vertrieb` | 3.4b (Neufassung v01) | **live** (2.5) · **FAQ-Abschnitt** vor dem Abschluss-CTA (Briefing 0030, Nachtrag 09.09.2026): geteilter Baustein `components/site/FaqSection.tsx` mit `topic: "wachstum"`, Überschrift „Häufige Fragen zu Wachstum und Vertrieb.", Eyebrow „Klarheit vor dem Gespräch" (beides Seitentext in der Content-Datei). Zu diesem Thema liegt **noch keine Frage** in Sanity – der Abschnitt entfällt deshalb vollständig, ohne Leerfläche | keine – die Oberflächen (Kampagnen-Cockpit, Instagram-Anzeige, Such-/Feed-/Zeitleisten-Visuals, Geräterahmen der Kampagnen-Geschichte, Landingpage-Fächer) sind bewusst **schematische Illustrationen** (kein `Shot`, kein echter Screenshot, kein Foto nötig) |
| `/clubprozesse` | 3.5b (Neufassung v01) | **live** (2.6) · **FAQ-Abschnitt** vor dem Abschluss-CTA (Briefing 0030, Nachtrag 09.09.2026): geteilter Baustein `components/site/FaqSection.tsx` mit `topic: "clubprozesse"`, Überschrift „Häufige Fragen zu den Clubprozessen.", Eyebrow „Klarheit vor dem Gespräch" (beides Seitentext in der Content-Datei). Zu diesem Thema liegt **noch keine Frage** in Sanity – der Abschnitt entfällt deshalb vollständig, ohne Leerfläche | keine – die Oberflächen (Clubwebsite mit Platzstatus + News, Sonntags-Log, Concierge-Chat, Platzstatus-Toggle, Gastfee-Zahlung, Bericht-Strecke, Captains-App-Demo) sind bewusst **schematische Illustrationen** (kein `Shot`, kein echter Screenshot, kein Foto nötig); **kein Modulstatus** (Badges/Legende/„Stand je Modul"-Zeile weggelassen), Praxis-Zeile „68 % Rehburg-Loccum" bleibt |
| `/pakete` | 3.7 | **live** (2.1) · **FAQ aus Sanity** (3.6, Briefing 0030): die sieben Fragen kommen aus `faq` (`topic: "pakete"`), sortiert nach `order`, gebaut mit dem geteilten Baustein `components/site/FaqSection.tsx`; `paketeFaq` ist aus `content/pakete.ts` gelöscht, es gibt **keinen Rückfall** auf das Repo. Liefert Sanity nichts, entfällt der FAQ-Abschnitt vollständig – ohne Leerzustandsmeldung. Das Akkordeon bleibt `<details>/<summary>`, alle Antworten stehen im Server-HTML. `FAQPage`-JSON-LD folgt mit 6.4 | – (Fassung 2 hat keine Screenshots) |
| `/ueber-golfnext` | 3.8b (Neufassung v01) | **live** (2.7) · **FAQ-Abschnitt** vor dem Abschluss-CTA (Briefing 0030, Nachtrag 09.09.2026): geteilter Baustein `components/site/FaqSection.tsx` mit `topic: "allgemein"`, Überschrift „Häufige Fragen zu GolfNext.", Eyebrow „Klarheit vor dem Gespräch" (beides Seitentext in der Content-Datei). Fläche **Sand** statt Mist, weil der Wissen-Abschnitt davor selbst entfällt, solange kein Artikel veröffentlicht ist – sonst stünde Mist auf Mist. Zu diesem Thema liegt **noch keine Frage** in Sanity – der Abschnitt entfällt deshalb vollständig, ohne Leerfläche | Porträts Fred und Stefan (Hero + „Menschen dahinter“) als beschriftete Platzhalter, Partnerlogos + Freigaben (Club-Kacheln „Logo folgt“) – Fred liefert; **der Wissen-Slider ist kein offenes Bild mehr** (3.9, Briefing 0029) – er zeigt die vier neuesten Artikel aus Sanity, ohne Rubrik-Filter; Titelbilder pflegt die Redaktion im Studio, sonst steht der `Shot`-Platzhalter „Bild folgt“; **kein Modulstatus**: Grundsatz /02 „Wir versprechen nur, was läuft.“ + Status-Aufzählung/-Legende **nicht gebaut** (Überschrift „Zwei Grundsätze“), „Pilotclub“-/Entwicklungspartner-Wortlaut bleibt wortgleich |
| `/impressum`, `/datenschutz` | `docs/legal/impressum.md`, `docs/legal/datenschutz.md` | **live** (5.1 und 5.2, Briefing 0028) – die Markdown-Dateien werden **beim Bauen** gelesen (`lib/legal.ts`, Routen `force-static`); `docs/legal/` bleibt damit die einzige Wahrheit, es gibt keine Kopie unter `content/`. Interne `>`-Vermerke und `[[ … ]]` filtert der Leser heraus, der Parser bricht bei unbekannter Markdown-Auszeichnung ab. Kein Hero, kein Eyebrow (dafür gibt es keinen freigegebenen Text), kein Abschluss-CTA über einem Rechtstext; ein dezenter Reveal, Endzustand im Server-HTML. Fließtext max. 70 Zeichen (`components/pages/recht/Rechtstext.module.css`). **Ohne `noindex`** – beide Seiten müssen auffindbar sein; damit werden „Impressum" und „Datenschutz" in der Fußleiste und der Einwilligungs-Link im Kontaktformular automatisch zu echten Links (`isLinkable`). **Der Umsatzsteuer-Absatz fehlt bewusst** (Entscheidung Stefan, 07.09.2026): Die alte Angabe war eine Steuernummer, keine USt-IdNr.; § 5 DDG verlangt sie nur, sofern vorhanden | – |
| `/praxis` (Unterpunkt von `/ueber-golfnext`, Adresse bleibt), `/praxis/[slug]`, `/praxis/thema/[slug]` | 3.9a (Übersicht) + 3.9b (Artikel), Neufassung v01 + Sanity | **gebaut** (3.4, Briefing 0027), Route bleibt vorerst `geplant`/`noindex` – der Wechsel auf `live` kommt, sobald Fred den ersten Artikel veröffentlicht hat (Briefing 0027, Frage 2). Der Blog ersetzt das entfallene `/ratgeber`. **Vier bewusste Abweichungen von Mock und docs/04:** (1) **kein Newsletter-Block** „Praxis-Post" und kein Hero-CTA „Praxis-Post abonnieren" – kein E-Mail-Feld ohne Versandweg (Entscheidung Stefan, 07.09.2026); (2) **Blättern per Link** `?seite=2` („Ältere Beiträge") statt „Ältere Beiträge laden" – nur so ist die Liste ohne JavaScript bedienbar; (3) **Themenfilter über `/praxis/thema/<slug>`** statt des Suchparameters `?rubrik=` aus docs/04 – zwei Wege auf dieselbe Liste wären doppelter Inhalt, und ein Link ist ohne JavaScript bedienbar; (4) **kein `loading.tsx`** – eine Suspense-Grenze lässt Next streamen, ohne JavaScript bliebe dann das Skelett stehen (gemessen, docs/entscheidungen.md 08.09.2026). **Drei weitere Abweichungen aus Stefans Preview-Durchgang (08.09.2026):** (5) die **Autorenzeile in der Seitenspalte** entfällt – der Autor stand sonst dreimal auf einer Seite; ohne Inhaltsverzeichnis entfällt die Spalte ganz und der Text nimmt die volle Breite; (6) **„Passt dazu" wird unter 1000 px ein Querscroller** (reines CSS mit `overflow-x` und `scroll-snap`, kein Karussell, ohne JavaScript bedienbar, Scroll im eigenen Container); (7) **Autorenfotos statt Initialenkreis** in Kacheln und Artikelkopf – die Initialen bleiben der Rückfall für Autoren ohne Porträt. Ebenfalls weggelassen: Lesefortschrittsbalken, Schlagwortzeile, Seitenspalten-Kasten `.cta2` und „Alle Beiträge von …" (keine Autorenseite). | Titelbilder, Artikelbilder und Autorenporträts kommen aus Sanity; fehlen sie, steht der beschriftete `Shot`/`Portrait`-Platzhalter („Titelbild folgt" / „Bild folgt"); **kein Modulstatus** |
| `/kontakt` (Unterpunkt von `/ueber-golfnext`) | 3.10 | **live** (4.3, Briefing 0025) – Layout aus Mock 3.10, Formular mit Server Action, zod und zweistufigem Spam-Schutz nach docs/06; ohne JavaScript absendbar. Ziel des CTA-Fallbacks aus `lib/links.ts`; **„Kontakt" erscheint damit im Über-GolfNext-Dropdown** | Porträt Fred (Seitenspalte) als Platzhalter; Anschrift noch offen („Straße und Hausnummer folgt", „PLZ und Ort folgt" – wortgleich aus dem Mock, kommt mit 5.2) |
| 404 (jede unbekannte URL) | kein Mock | **gebaut** (2.9, Briefing 0022) – `app/not-found.tsx` mit Header, Footer, Startseite + Erstgespräch; Texte aus `lib/ui/messages.ts` | – |
| Fehlerseiten | kein Mock | **gebaut** (2.9, Briefing 0022) – `app/(site)/error.tsx` (reset()) und `app/global-error.tsx` | – |
| `/danke` | kein Mock | **gebaut** (5.4, Briefing 0033) – Bestätigungsseite nach einer erfolgreichen Formularanfrage und Zielpunkt der URL-basierten Conversion (`/danke?quelle=kontakt`). Status `system` und `noindex`: Man kommt nur über das gesendete Formular hierher, die Seite gehört in keine Navigation, keine Sitemap und keinen Suchindex. **Kein eigener freigegebener Text** – Überschrift und Bestätigungssatz sind die Erfolgsmeldung aus `lib/forms/messages.ts`, der Rückweg kommt aus `lib/ui/messages.ts`; der Folge-Link zur Terminwahl erscheint erst mit echtem Buchungsweg. Der Sprung dorthin passiert nur bei erteilter Einwilligung **und** gesetzter GTM-ID – sonst bleibt es beim Inline-Erfolg auf `/kontakt` (docs/09) | – |
| `/studio` | Sanity Studio | offen | – |

## Weiterleitungen (bestehende golfnext.de)

Bekannt: `/impressum/` → `/impressum`. **Erledigt seit Briefing 0028** – Next leitet mit `trailingSlash: false` von selbst um (308), es braucht dafür keinen Eintrag in `next.config.ts`; die Prüfung steht in `tests/e2e/rechtstexte.spec.ts`. Vollständige URL-Liste der alten Website erheben (Crawl oder Search Console) **[S]** und hier ergänzen.

## CTA-Ziele (aus `.env`)

- Online-Erstgespräch → `NEXT_PUBLIC_BOOKING_URL` (bestehender Buchungsweg **[F]**)
- ~~Live-Demo → `NEXT_PUBLIC_LIVE_DEMO_URL`~~ – entfallen am 08.09.2026 (Briefing 0031, Masterplan 2.11).
  Es gibt keine Live-Demo; das Erstgespräch ist überall die einzige Aktion. Die Mocks zeigen sie
  weiterhin und sind an diesen Stellen überholt.
- Pakete vergleichen → `/pakete#vergleich`
